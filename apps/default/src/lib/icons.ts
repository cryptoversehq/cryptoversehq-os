/**
 * Icon barrel for app code - always import icons from '@/lib/icons'.
 *
 * Every lucide-react icon is re-exported here, so ANY real lucide icon name
 * resolves (Swords, ScrollText, Trophy, LayoutDashboard, ...). Icon names are
 * PascalCase and must exist in lucide-react - an invented name fails the whole
 * app build at compile time. If you are not certain an icon exists, use a
 * common one (Activity, Star, Zap) instead of guessing an exotic name.
 */
export * from 'lucide-react';
